import 'package:app_6sib1/widget/my_widget.dart';
import 'package:flutter/material.dart';

class MyInformasi extends StatelessWidget {
  const MyInformasi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: MyTextBesar(text: 'Layer Informasi'),
    );
  }
}
